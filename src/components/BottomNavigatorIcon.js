import React from 'react';
import Icon from 'react-native-vector-icons/FontAwesome5';

const BottomNavigatorIcon = ({name, color, size}) => {
  return <Icon name={name} color={color} size={size} />;
};

export default BottomNavigatorIcon;
